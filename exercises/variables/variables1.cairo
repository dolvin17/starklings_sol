// variables1.cairo
// Make me compile!
// Execute `starklings hint variables1` or use the `hint` watch subcommand for a hint.

use debug::PrintTrait;


fn antoher_function(x: felt252)
{
    x.print();
}
fn main() {
    antoher_function(5);
}
